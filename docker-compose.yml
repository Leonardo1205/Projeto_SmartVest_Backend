services:
  db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_USER: smartvest
      POSTGRES_PASSWORD: smartvest
      POSTGRES_DB: smartvest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
